import Vue from 'vue'
import gpBtn from "./button";
import gpDialog from "./dialog";
import gpInput from "./input";
import gpMsg from './msg/msg.vue'
import gpCard from './card'
import gpPagination from './pagination'
import gpTable from './tabel'
import gpIcon from './icons'

Vue.use(gpBtn)
Vue.use(gpDialog)
Vue.use(gpMsg)
Vue.use(gpInput)
Vue.use(gpCard)
Vue.use(gpPagination)
Vue.use(gpTable)
Vue.use(gpIcon)
