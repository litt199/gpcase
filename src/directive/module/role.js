
export default {
  inserted: function (el, binding, vnode) {
    let role = binding.value
    /*if(role){
     const hasPermission =
      // 是否拥有权限
      if(!hasPermission){
        el.remove() //没有权限则删除模块节点
      }
    }*/
  }
}
